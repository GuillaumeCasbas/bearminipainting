// Unit code format: only letters, numbers and hyphens are allowed.
// Single source of truth shared by the CreateUnit use case and the frontend validation.
export const UNIT_CODE_REGEX = /^[a-zA-Z0-9-]+$/;
