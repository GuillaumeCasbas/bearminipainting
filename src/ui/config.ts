// Base path for the app. Must match the `base` option in vite.config.ts.
// Used for router basename so routes work under GitHub Pages (subpath) and locally.
export const BASE_PATH = '/bearminipainting/';
