/**
 * Shared UI constants for MiniPaint application
 */

// Completion rate thresholds for progress bar coloring
export const COMPLETION_RATE_RED_THRESHOLD = 20;
export const COMPLETION_RATE_GREEN_THRESHOLD = 80;
